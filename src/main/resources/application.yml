spring:
  # 数据库配置
  datasource:
    url: jdbc:mysql://localhost:3306/boss_app?characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  type-aliases-package: cn.nean.boss.model
  mapper-locations: classpath:mapper/*.xml